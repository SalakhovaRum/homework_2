def decrease_mana(function_to_decorate)
    def wrapper(cat):
        print('до атаки:', car2.name, cat2. hp )
        attack (cat)
        print('после атаки':)


class Cat:
    def __init__(self,color,name,mana):
        self.name = name
        self.damag = damag
        self.mana = mana
        self.hp = hp

    @decrease_mana
    def attack(self, cat :Cat):
        cat2.hp -= self.damag

cat1 = cat ('cat1',5 , 10, 100)
cat2 = cat ('cat 2', 2, 5 , 200)


    Используется в классах и ООП:
Статичный метод:
@staticmetod
def print_random(a,b):
    print('Random digit:',random.randint(a,b))
    crit = 1
    if random_number >50:
        print ("Выпал крит")
        crit = 2
    else:
        print ('Крит не выпал')
    return crit


Метод isinstance():






